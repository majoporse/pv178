@page "/sign-in/create-gym"
@layout SigninLayout
@using BlazorServerAuthenticationAndAuthorization.Commons
@using BlazorServerAuthenticationAndAuthorization.models
@inject NavigationManager navigationManager

<MudPaper Elevation="0" Style="width: 40%">
	<CreateGym onsubmit="@(OnValidSubmit)" />
</MudPaper>

@code {
	private void OnValidSubmit(RegisterGymForm model)
	{
		Console.WriteLine(model.ToString());
		navigationManager.NavigateTo("/home");
	}
}
